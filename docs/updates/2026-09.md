# 2026-09 update log

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md` and this `docs/updates/` index. `.claude/PROGRESS.md` became the root `progress.md` with open items only; the rules it held (deliberately removed features, environment notes) moved verbatim into `CLAUDE.md`, and two records moved here as U-20260922-02 and U-20260922-03. `Update Note.txt` stays the Steam announcement draft.
- **Files**: `CLAUDE.md`, `.claude/PROGRESS.md` → `progress.md`, `architecture_explore.md`, `architecture.md`, `docs/updates/`.
- **Open items**: see `progress.md`.

## U-20260922-02 · 2026-09-22 · Codacy stalls on very large PRs (PR #216) · #incident #ci

> Moved from the former `.claude/PROGRESS.md` (section 已知但刻意保留); the incident itself happened on PR #216.

- **Codacy 對大 PR 會卡住**（PR #216，68 檔案 / +9087 −5290，最後帶著紅的 Codacy 合併）。
  症狀：check 一直是 `action_required`，但它自己回報 0 個 issue、0 個 annotation、
  summary 空白，而且 **PR 的 `files` 端點回傳 `total: 0`**——它認為這個 PR 沒有變更
  檔案，所以算不出 diff。同一個 commit 在 **commit 層級**的 gate 是過的
  （`newIssues: 0`、`isUpToStandards: true`），所以不是程式碼問題。
  試過無效：關掉再重開 PR、推新 commit（三次，每次都真的重新分析）、
  `POST check-runs/{id}/rerequest`（404）、`POST .../reanalyze`（404）。
  要清掉需要帳號層級權限（Codacy 網頁的 Re-analyse 或帳號 api-token）。
  判斷方法：完整的 PR 分析會有 `isUpToStandards` / `newIssues` / `quality` / `coverage`，
  卡住的只有 `analyzable: true`。

## U-20260922-03 · 2026-09-22 · SonarCloud S6549 on pet sound paths marked WONTFIX · #decision #sonarcloud

> Moved from the former `.claude/PROGRESS.md` (section 已知但刻意保留).

- SonarCloud 上有一筆 `pythonsecurity:S6549`（寵物音效路徑）標成 **WONTFIX** 並附了理由。安全類規則不吃 `# NOSONAR`，只能走 API 或網頁標記（見 `CLAUDE.md` Environment notes）。

## U-20260922-04 · 2026-09-22 · Point project URLs at JeffreyChen-SteamProjects/FrontEngine · #done #metadata

- **What**: `Homepage` and `Code` in `pyproject.toml` and `stable.toml`, and the download link in `LEARN.md`, pointed at the misspelled old owner `Intergration-Automation-Testing`; they now name the actual origin, `JeffreyChen-SteamProjects/FrontEngine`. Closes `progress.md` #3 (workspace X-11).
- **Decision**: #3 was marked [DECIDE] because the PyPI page shows these links. The old links pointed at an owner that does not exist under that spelling, so the default taken was to follow `origin`; the PyPI page changes with the next release of each channel.
- **Files**: `LEARN.md`, `pyproject.toml`, `stable.toml`, `progress.md`.
- **Open items**: none.

## U-20260923-01 · 2026-09-23 · FrontEngine.log moves out of the working directory · #done #logging

- **What** (workspace item X-6): `frontengine/utils/logging/loggin_instance.py` opened `FrontEngine.log` in the working directory at import (falling back to the home directory, then to no file). JEditor and PyBreeze embed FrontEngine, so every process that imported it left a `FrontEngine.log` wherever it started; `D:\Codes\JEDITOR` and the workspace root had collected them. The handler also rotated inside `emit()` at 1 GiB, which fails on Windows while another process holds the file.
- **Now**, the same design as AutoControlGUI (`5714a59`) and JEditor: `$FRONTENGINE_LOG_FILE` or `~/.frontengine/logs/FrontEngine.log`; the package handler is built with `delay=True`, so importing writes nothing; append mode with the process id on each line; rotation to `.1` past 10 MiB only when a process opens the file; UTF-8 with `errors="backslashreplace"`; an unopenable path falls back to `os.devnull` with one `RuntimeWarning` (the old candidate chain existed so a read-only launch directory could not stop the app; that guarantee holds). `_build_file_handler` and `LOG_FILE_NAME` are gone; nothing else used them.
- **Tests**: `tests/unit_test/test_log_location.py` (9, same cases as JEditor's): home default, environment override, loading the module alone in a fresh interpreter leaves the cwd empty, first record creates the directory and later handlers append, text outside cp950 and a lone surrogate survive, an unopenable path only warns, the package handler's path, mode and encoding. `py -m pytest tests/ -q` 1184 passed; `pyflakes frontengine/ exe/ tests/` silent.
- **Branch**: `dev` was an ancestor of `main` (three main-only doc commits: `289aa7c`, `54a2028`, `2adaddb`); it was fast-forwarded to `main` before this commit, as `CLAUDE.md` describes for a release that could not move `dev`.
- **Files**: `frontengine/utils/logging/loggin_instance.py`, `tests/unit_test/test_log_location.py`, `architecture_explore.md`.
- **Open items**: none here; WebRunner remains under workspace X-6.

## U-20260923-02 · 2026-09-23 · PySide6 6.11.2; Dependabot targets dev · #done #deps #ci

- **What** (workspace item X-1 and X-16, `progress.md` #7): PySide6 moves to `==6.11.2` in `pyproject.toml` (dev channel), `stable.toml` and `requirements.txt`, taking Dependabot PR #217. 6.11.2 carries the `QAction.menu()` wrapper-invalidation fix the other Qt projects in the workspace are waiting for.
- **Order**: the published `je_editor` and `frontengine` pin PySide6 exactly and PyBreeze pins the same version, so the three move in sequence. JEditor went first (its `96de85e` on dev); FrontEngine follows here; PyBreeze bumps once both are published.
- **Dependabot** now sets `target-branch: "dev"` in `.github/dependabot.yml`: PRs opened against `main` would publish on merge, which is why #217 sat since 2026-08-21.
- **Checks** with 6.11.2 installed: `pytest tests/ -q` 1184 passed (headless); `pyflakes frontengine/ exe/ tests/` silent.
- **Files**: `pyproject.toml`, `stable.toml`, `requirements.txt`, `.github/dependabot.yml`, `progress.md`.
- **Open items**: none here.

## U-20260923-03 · 2026-09-23 · pyflakes in dev_requirements; stable.toml header; system_tray location · #done #docs

- **What** (`progress.md` #4, #5, #6):
  - `pyflakes` is now in `dev_requirements.txt`; the README's static-check section says so instead of asking for a separate install. The same file still pinned `PySide6==6.11.1` after the 6.11.2 move (U-20260923-02) and now matches.
  - `stable.toml` opened with "Rename to dev version / This is dev version" although it builds the stable `frontengine` package; the comment now names its channel.
  - `architecture_explore.md` listed `system_tray/extend_system_tray.py` in the `frontengine/ui/` table and drew it among the `ui/*` boxes; the package is `frontengine/system_tray/`, and both places now say it sits outside `ui/`.
- **Checks**: `pytest tests/ -q` 1184 passed (headless).
- **Files**: `dev_requirements.txt`, `README.md`, `stable.toml`, `architecture_explore.md`, `progress.md`.
- **Open items**: none.

## U-20260924-01 · 2026-09-24 · Move CI to Node 24 actions pinned by commit · #ci #security #deps

- **What**: the workflows still used Node 20 actions, and GitHub removed Node 20 from its runners on 2026-09-23. They were also referenced by mutable tags, which the 2025 tj-actions/changed-files compromise showed can be repointed. All 5 references in `ci.yml`, `release.yml` now name the latest release, whose `action.yml` declares `node24` (or is composite), pinned to its commit SHA with the version as a comment: checkout v7.0.1, setup-python v7.0.0, upload-artifact v7.0.1. No breaking change in the release notes applies: checkout v6 keeps the credentials in a separate included file that later git commands still read, and v7 blocks fork checkouts under `pull_request_target` / `workflow_run`, which are not used; setup-python v7 removed the unused `pip-install` input. `.github/dependabot.yml` gains a weekly `github-actions` entry on `dev`, so Dependabot moves each pin and its comment together (workspace `progress.md` X-21).
- **Tests**: `tests/test_workflow_actions.py` requires every remote `uses:` to name a 40-hex commit followed by a `# vX.Y.Z` comment, each action at a single commit across workflows, and `dependabot.yml` to cover pip and GitHub Actions on `dev`. The previous workflows fail it.
- **Result / numbers**: the new tests pass. The workflows take effect on the next push.
- **Sources**: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/ and each action's release notes (`gh api repos/<owner>/<name>/releases`).
- **Files**: `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.github/dependabot.yml`, `tests/test_workflow_actions.py` (new).

## U-20260924-02 · 2026-09-24 · Keep checkout credentials only in the job that pushes · #ci #security

- **What**: zizmor 1.30.1 (`zizmor --offline .github/workflows`) reported that `actions/checkout` leaves the job token in `.git/config` (artipacked), where every later step can read it. The 1 checkouts in jobs that never push now set `persist-credentials: false`. The release job that pushes the version bump and tag (1 checkout) now says `persist-credentials: true  # this job pushes`, so the exception is visible.
- **Tests**: `tests/test_workflow_actions.py` gains a check that every checkout step sets `persist-credentials` explicitly. The previous workflows fail it.
- **Result / numbers**: zizmor reports no warnings or errors for these workflows any more; the test file passes.
- **Files**: `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `tests/test_workflow_actions.py`.
